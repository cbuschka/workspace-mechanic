#!/bin/bash

MECHANIC_INSTALLATION_DIR=$(cd `dirname $0`/.. && pwd)

MECHANIC_CLASSPATH=
for f in $MECHANIC_INSTALLATION_DIR/lib/*.jar; do
  MECHANIC_CLASSPATH=$MECHANIC_CLASSPATH:$f
done

java -cp $MECHANIC_CLASSPATH -jar $MECHANIC_INSTALLATION_DIR/lib/mechanic.jar "$@"